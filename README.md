# Virtual Board — MVP

Acest repository conține scaffold minimal TypeScript + React pentru o tablă virtuală folosită la meditații.

Pași pentru rulare locală:

```bash
npm install
npm run dev
```

Fișiere importante:

- [src/components/CanvasBoard.tsx](src/components/CanvasBoard.tsx)
- [src/App.tsx](src/App.tsx)

Următorii pași pe care îi pregătesc: configurarea hook-urilor de commit (Husky + commitlint + Commitizen), ESLint + Prettier, și commit-uri mici cu mesaje Conventional Commits.
